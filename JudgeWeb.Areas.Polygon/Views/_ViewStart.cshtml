@{
    Layout = "_Layout";
    ViewData["BigTitle"] = "Polygon";
    if (User.IsInRole("Administrator") &&
        (string)ViewContext.RouteData.Values.GetValueOrDefault("controller") == "Root")
    {
        ViewData["AjaxUpdateUrl"] = "/dashboard/updates";
    }
}
